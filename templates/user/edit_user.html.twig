{% extends "base.html.twig" %}

{% block title 'Modifier un user' %}

{% block body %}

<div class="container">
    <div class="container_from">

        <h1>Modifier mon compte</h1>

        <div class="container_form">

            {{ form_start(formUser, { 'attr' : { 'class' : 'form' }} ) }}

                <div class="div_form">

                    {# <div class="form-error">
                        {{ form_errors(formUser.lastName) }}
                    </div> #}

                    <label for="inputUsername" class="form_label">Nom d'utilisateur</label>

                    <div>
                        {{ form_widget(formUser.lastName, {attr : {class : 'form_input' }} ) }}
                    </div>
                    
                    <small class="error_input_small">{{ form_errors(formUser.lastName) }} </small>

                </div>

                <div class="div_form">

                    {# <div class="form-error">
                        {{ form_errors(formUser.firstName) }}
                    </div> #}

                    <label for="inputUsername" class="form_label">Prénom d'utilisateur</label>

                    <div>
                        {{ form_widget(formUser.firstName, {attr : {class : 'form_input' }} ) }}
                    </div>
                    
                    <small class="error_input_small">{{ form_errors(formUser.firstName) }} </small>

                </div>

                <div class="div_form">

                    <label for="inputPhoneNumber" class="form_label">{{ form_label(formUser.phoneNumber) }}</label>

                    <div>
                        {{ form_widget(formUser.phoneNumber, {attr : {class : 'form_input' }} ) }}
                    </div>
                    
                    <small class="error_input_small">{{ form_errors(formUser.phoneNumber) }} </small>
                </div>

                <div class="div_form">

                    <label for="inputEmail" class="form_label">{{ form_label(formUser.email) }}</label>

                    <div>
                        {{ form_widget(formUser.email, {attr : {class : 'form_input' }} ) }}
                    </div>
                    
                    <small class="error_input_small">{{ form_errors(formUser.email) }} </small>
                </div>

                {# <div class="div_form">
                    <label for="inputImage">Photo de profil (Facultatif)</label>
                    {{ form_widget(form.image.file, {attr : {class : 'form_input'} } ) }}
                    {{ form_errors(form.image.file) }}
                </div> #}

                <button type="submit" class="btn">Modifier</button>

            {{ form_end(formUser) }}

        </div>
    </div>
</div>
{% endblock body %}